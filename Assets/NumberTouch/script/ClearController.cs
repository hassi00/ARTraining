using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClearController : MonoBehaviour
{
    /*
    public GameObject clearStarPrefab;
    private Vector3 NumbersPosition;

    private void Start()
    {
        NumbersPosition = transform.position;
    }
    // Update is called once per frame
    void Update()
    {
        
        if(TouchController.number >= 10)
        {
            Invoke("ClearTraining",1.0f);
        }
        

    }

    void ClearTraining()
    {
        this.gameObject.SetActive(false);
        Instantiate(clearStarPrefab, transform);
    }
    */
    
}
